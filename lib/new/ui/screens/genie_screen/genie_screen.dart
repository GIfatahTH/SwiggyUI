import 'package:flutter/material.dart';

class GenieScreen extends StatelessWidget {
  const GenieScreen({Key? key}) : super(key: key);
  static const routeName = 'GenieScreen';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
