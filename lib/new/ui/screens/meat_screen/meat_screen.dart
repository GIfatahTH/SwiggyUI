import 'package:flutter/material.dart';

class MeatScreen extends StatelessWidget {
  const MeatScreen({Key? key}) : super(key: key);
  static const routeName = 'MeatScreen';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
